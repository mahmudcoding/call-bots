# Copy to users.yaml (gitignored) and fill in real staging accounts.
# Requirements per account:
#   - member of the workspace the call belongs to
#   - two-factor auth disabled
# label: shown inside that user's published video and used for fixture/state
#        file names — keep it short, letters/digits only.

baseUrl: https://airion-cargo.store

# Optional; only needed for `calls-sim create` (or pass --ws).
workspace: WQASTGL05A1

users:
  - email: qa.stg.l05.member-a@example.com
    password: "CHANGE-ME"
    label: Alice
  - email: qa.stg.l05.member-b@example.com
    password: "CHANGE-ME"
    label: Bob
  - email: qa.stg.l05.member-c@example.com
    password: "CHANGE-ME"
    label: Carol
  - email: qa.stg.l05.member-d@example.com
    password: "CHANGE-ME"
    label: Dave
